export const AppConfig = {
    // CRITICAL: Use relative path (./) to route through xs-app.json on Workzone
    // Example: "./api/{{NAMESPACE}}/SERVICE_NAME"
    API_BASE_URL: "./api/{{NAMESPACE}}/{{SERVICE_NAME}}",
    ENDPOINTS: {
        // Define your endpoints here
        // EXAMPLE: "/Entities"
    }
};
